# JSON Filter Adults Task

## 🧩 Overview

In this task, you will process a JSON dataset containing information about multiple individuals and filter out only those who are considered adults (age ≥ 18).

The goal is to demonstrate your ability to:

- Read and parse structured JSON data
- Apply filtering logic
- Generate a correctly formatted output file
- Work with file paths inside a containerized environment

This task simulates a real-world data processing workflow commonly used in backend systems and ETL pipelines.

---

## 📂 Input Data

The input file is located at:

/app/input.txt

The file contains a JSON array of objects with the following structure:

```json
[
  {"name": "Alice", "age": 22},
  {"name": "Bob", "age": 16},
  {"name": "Charlie", "age": 19}
]
